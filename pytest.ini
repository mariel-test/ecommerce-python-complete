[pytest]
addopts = --html=python-report/report.html --self-contained-html
testpaths = tests
markers =
    smoke: 8 TC críticos — 1 por módulo + E2E-01. Si alguno falla el release está bloqueado.
    happy_path: 25% triage — camino feliz de cada módulo + E2E-01 y E2E-04.
